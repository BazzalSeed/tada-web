So i think we should get rid of the code thing for now. delete the table and all code refs, we dfeer it to v1. add to v1.md first bullet point in notes section

does neon db have a way for us update row value? if so, we should just have a db_column to be is_admin instead of this hacky way to determine admin

For admin, also record 
# Notes (to be parsed into actual V1 milestones)
1. code-based invite system and flesh out how do we tie plan to code-invited test users. 