// ============================================================================
// lib/core barrel — the Backend-owned runtime implementations of the frozen
// flow signatures (lib/contracts/filter.ts). Consume via `import { … } from
// "@/lib/core"`; import the types from "@/lib/contracts".
// ============================================================================

export * from "./flow";
